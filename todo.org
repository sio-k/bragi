Bug/Mac: proper handling of left and right alt. Might need to internally rename the modifiers like Emacs does to Ctrl and Meta. Using Meta as the secondary modifier key. Meta would mean Alt for Windows/Linux and Alt for Mac. Also proper handling of the Command key. Check how Emacs handles CMD in Mac.

Bug/Cursors: while in multiple cursors, and having a cursor at the end of a line and the another at the beginning of the following line, writing acts weirdly because it inserts the rune correctly for the lowest offset cursor but it doesn't respect the line feed and insert the next rune within an offset of 1, multiple inserts makes the highest offset cursor insert the text from right to left.

Improvement/Buffer: make the buffer piece table to actually store the line starts and cache them. This is a big improvement for bigger files as after the first load we might just not need to fully calculate lines again. Maybe we can also cache the pieces that have changed.

Improvement/Search: save the search history, or at least the last term that was used for searching so the user can press search twice and automatically populate.

Feature/Marks: save pane/buffer marks for some user actions. Show the existing marks on the pane fringe

Maybe Bug/Cursor: when converting the cursor offset to coords and back, I'm using the passed line information. This might not be a good idea, since the only time we care about coords might be when we do something in the view, and then it should always use the pane lines (that could be the wrapped lines or regular buffer lines) but not an arbitrary line information.

[Unimporant] Code Style/-disallow-do: analyze if it's a good idea to get rid of the inline if statement with do. It's a good feature for me but at this point I have too many things happening in single lines sometimes and might just be better to have code style a little bit different

Improvement/Window Resizing And System Display Scale: if the user changes their display scale or is currently resizing the window, we need to reconfigure things accordingly. Currently we're resizing things every frame and also this affects the DPI scale. Make this consistent before re-enabling it.

Improvement/Speed/Piece Table Cache Absolute Line Starts And Buffer Positions: in order to not recreate the line offsets and collect pieces for when we do multi-line effect operations, making the Piece Table cache the absolute line offsets would make all this work easier and faster. Current speeds aren't that bad but we can make it a LOT better.

Bug/Line Wrappings: when calculating where the line wrap is, we go back to get a full word to wrap, but if the word is too long (currently 20 characters long is too long), we just wrap on the spot. This wrap is causing a bounds issue where one of the characters is caught in nowhere and will not render. A little bit of an edge case but it matters.

  ------------------
 /   QUICK ONES   /
------------------
